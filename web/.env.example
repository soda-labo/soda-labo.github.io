# Copy this file to `.env` and fill in your secrets.
# `.env` is gitignored — never commit it.

# Optional: Semantic Scholar API key (free, https://www.semanticscholar.org/product/api)
# Speeds the abstract fallback from ~3 sec/request to 1 sec/request.
SEMANTIC_SCHOLAR_API_KEY=
